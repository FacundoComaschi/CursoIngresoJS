function mostrar()
{
	//tomo la edad 
	var edad;
	edad = txtIdEdad.value;
	if (edad <=17){
		if(edad >=13)
		alert ("Es adolescente")
		
	} else {
		alert ("no es adolescente")
	}


}//FIN DE LA FUNCIÓN
// operadores logicos && || !
//ej if (edad>= 13 && edad <=17)S 
// en el && me devuelve verdadero solo si los dos son true
// true false = false
// en el || me devuelve false solo si ambos son false
//true false = true