/*
al presionar el botón mostrar 10 repeticiones 
con números ASCENDENTE, desde el 1 al 10.*/
function mostrar()
{
	var contador = 0;

	while (contador < 10) {
		contador = contador + 1;
		console.log ("contador" + contador);

		//contador = variable + constante
		//contador = contador + 1;
		//contador += 1;
		//contador++; utiliza el valor anterior y luego le suma 1(posincremento)
		//++contador; le suma 1 al valor anterior(preincremento)
		//acumulador = variable mas otra variable
		//acumulador = acumulador + otraVariable
		//continue
		//break
	
	}

}